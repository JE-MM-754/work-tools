# OpenClaw API Configuration
# Set these to connect to your OpenClaw instance (via tunnel or direct URL)
# Without these, the dashboard runs in simulation/demo mode
OPENCLAW_API_URL=http://localhost:18789
OPENCLAW_AUTH_TOKEN=your-auth-token-here
